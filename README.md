# project_algo_trading
for project submission Jun '25

# Docker and Database setup
cd /mnt/e/Repos/project_algo_trading && chmod +x scripts/*.sh
cd /mnt/e/Repos/project_algo_trading && ./scripts/setup_docker.sh

# check if container is running
cd /mnt/e/Repos/project_algo_trading && ./scripts/check_container.sh